---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/HeaderNew.astro';
import { getTranslations } from '../i18n/translations';
import { BookOpen, Bot, Palette, DollarSign } from '@lucide/astro';

const t = getTranslations('es');
---

<!doctype html>
<html lang="es">
	<head>
		<BaseHead title={t.site.title} description={t.site.description} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<h1><BookOpen size={48} style="display: inline; margin-right: 1rem; vertical-align: middle;" /> {t.home.title}</h1>
				<p class="hero-subtitle">
					{t.home.subtitle}
				</p>
				<div class="cta-buttons">
					<a href="https://wallu.app" target="_blank" class="btn-primary">{t.home.startFree}</a>
					<a href="/blog" class="btn-secondary">{t.home.tutorials}</a>
				</div>
			</section>

			<section class="features">
				<h2>{t.home.whyChoose}</h2>
				<div class="features-grid">
					<div class="feature">
						<Bot size={32} class="feature-icon" />
						<h3>{t.home.features.ai.title.replace('🤖 ', '')}</h3>
						<p>{t.home.features.ai.description}</p>
					</div>
					<div class="feature">
						<Palette size={32} class="feature-icon" />
						<h3>{t.home.features.design.title.replace('🎨 ', '')}</h3>
						<p>{t.home.features.design.description}</p>
					</div>
					<div class="feature">
						<DollarSign size={32} class="feature-icon" />
						<h3>{t.home.features.monetization.title.replace('💰 ', '')}</h3>
						<p>{t.home.features.monetization.description}</p>
					</div>
				</div>
			</section>

			<section class="blog-preview">
				<h2>{t.home.blogPreview.title}</h2>
				<p>{t.home.blogPreview.description}</p>
				<a href="/blog" class="btn-outline">{t.home.blogPreview.viewAll}</a>
			</section>
		</main>
		<Footer />
		<style>
			main {
				max-width: 1200px;
				margin: 0 auto;
				padding: 2rem 1rem;
				box-sizing: border-box;
				overflow-x: hidden;
				width: 100%;
			}

			.hero {
				text-align: center;
				padding: 4rem 0;
				background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
				color: white;
				border-radius: 20px;
				margin-bottom: 4rem;
			}

			.hero h1 {
				font-size: 3rem;
				margin-bottom: 1rem;
				line-height: 1.2;
			}

			.hero-subtitle {
				font-size: 1.25rem;
				margin-bottom: 2rem;
				opacity: 0.9;
				max-width: 600px;
				margin-left: auto;
				margin-right: auto;
			}

			.cta-buttons {
				display: flex;
				gap: 1rem;
				justify-content: center;
				flex-wrap: wrap;
			}

			.btn-primary, .btn-secondary, .btn-outline {
				padding: 1rem 2rem;
				border-radius: 8px;
				text-decoration: none;
				font-weight: bold;
				transition: all 0.3s ease;
				display: inline-block;
			}

			.btn-primary {
				background: white;
				color: #667eea;
				border: 2px solid white;
			}

			.btn-primary:hover {
				background: transparent;
				color: white;
				transform: translateY(-2px);
			}

			.btn-secondary {
				background: transparent;
				color: white;
				border: 2px solid white;
			}

			.btn-secondary:hover {
				background: white;
				color: #667eea;
				transform: translateY(-2px);
			}

			.btn-outline {
				background: transparent;
				color: #667eea;
				border: 2px solid #667eea;
			}

			.btn-outline:hover {
				background: #667eea;
				color: white;
				transform: translateY(-2px);
			}

			.features {
				margin-bottom: 4rem;
			}

			.features h2 {
				text-align: center;
				font-size: 2.5rem;
				margin-bottom: 3rem;
				color: rgb(var(--black));
			}

			.features-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
				gap: 2rem;
				width: 100%;
				box-sizing: border-box;
			}

			.feature {
				padding: 2rem;
				border-radius: 12px;
				box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
				border: 1px solid #f0f0f0;
				text-align: center;
				box-sizing: border-box;
				width: 100%;
				max-width: 100%;
			}

			.feature-icon {
				color: #667eea;
				margin-bottom: 1rem;
			}

			.feature h3 {
				font-size: 1.5rem;
				margin-bottom: 1rem;
				color: rgb(var(--black));
			}

			.feature p {
				color: rgb(var(--gray-dark));
				line-height: 1.6;
			}

			.blog-preview {
				text-align: center;
				padding: 3rem;
				background: #f8f9fa;
				border-radius: 12px;
			}

			.blog-preview h2 {
				font-size: 2rem;
				margin-bottom: 1rem;
				color: rgb(var(--black));
			}

			.blog-preview p {
				font-size: 1.1rem;
				color: rgb(var(--gray-dark));
				margin-bottom: 2rem;
			}

			@media (max-width: 768px) {
				main {
					padding: 1.5rem 1rem;
					max-width: calc(100% - 2rem);
				}
				
				.hero {
					padding: 3rem 1rem;
					margin-bottom: 3rem;
				}
				
				.hero h1 {
					font-size: 2rem;
				}
				
				.hero-subtitle {
					font-size: 1rem;
				}
				
				.cta-buttons {
					flex-direction: column;
					align-items: center;
				}
				
				.features h2 {
					font-size: 2rem;
				}
				
				.features-grid {
					grid-template-columns: 1fr;
					gap: 1.5rem;
				}
				
				.feature {
					padding: 1.5rem;
					margin: 0 0.5rem;
				}
				
				.blog-preview {
					padding: 2rem 1rem;
					margin: 0 0.5rem;
				}
			}

			@media (max-width: 480px) {
				main {
					padding: 1rem 0.75rem;
					max-width: 100%;
					width: 100%;
					box-sizing: border-box;
				}
				
				.hero {
					padding: 2rem 1rem;
					margin-bottom: 2rem;
					border-radius: 12px;
					box-sizing: border-box;
					width: 100%;
				}
				
				.hero h1 {
					font-size: 1.8rem;
				}
				
				.hero-subtitle {
					font-size: 0.9rem;
					padding: 0 0.5rem;
				}
				
				.btn-primary, .btn-secondary, .btn-outline {
					padding: 0.8rem 1.5rem;
					font-size: 0.9rem;
					box-sizing: border-box;
				}
				
				.features-grid {
					grid-template-columns: 1fr;
					gap: 1rem;
					padding: 0;
				}
				
				.feature {
					padding: 1.5rem 1rem;
					margin: 0;
					width: 100%;
					box-sizing: border-box;
				}
				
				.feature h3 {
					font-size: 1.3rem;
				}
				
				.blog-preview {
					padding: 1.5rem 1rem;
					margin: 0;
					box-sizing: border-box;
					width: 100%;
				}
				
				.blog-preview h2 {
					font-size: 1.5rem;
				}
			}
		</style>
	</body>
</html>
